import { TextHoverEffectDirective } from './text-hover-effect.directive';

describe('TextHoverEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new TextHoverEffectDirective();
    expect(directive).toBeTruthy();
  });
});
